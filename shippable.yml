language: none

git:
  submodules: true

build:
  pre_ci_boot:
    image_name: dotmpe/testbox
    image_tag: latest # 0.0.4-bionic
    pull: true

  ci:
    -  sudo apt-get update && sudo apt-get install -y npm
    #-  npm -g install tap-xunit
    -  npm install -g git+https://github.com/dotmpe/tap-xunit.git
    -  export TERM=screen; ./sh-ci

# Id: U-S:shippable.yml
